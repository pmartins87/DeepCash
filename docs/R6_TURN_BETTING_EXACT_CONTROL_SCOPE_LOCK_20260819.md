# Scope lock

The branch intentionally stops at the exact-control foundation. It does not include two-street BR, R4 abstraction insertion, bounded-latency resolving, flop integration or production training.
