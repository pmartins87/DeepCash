name: DeepCash R6 posterior river representation bridge v1

on:
  workflow_dispatch:
  pull_request:
    paths:
      - 'tools/run_r6_posterior_river_representation_bridge_v1.py'
      - 'tests/test_r6_posterior_river_representation_bridge.py'
      - 'docs/R6_POSTERIOR_RIVER_REPRESENTATION_BRIDGE_PRECOMMIT_20260819.md'
      - '.github/workflows/r6-posterior-river-representation-bridge-v1.yml'
  push:
    branches: [main]
    paths:
      - 'tools/run_r6_posterior_river_representation_bridge_v1.py'
      - 'tests/test_r6_posterior_river_representation_bridge.py'
      - 'docs/R6_POSTERIOR_RIVER_REPRESENTATION_BRIDGE_PRECOMMIT_20260819.md'
      - '.github/workflows/r6-posterior-river-representation-bridge-v1.yml'

permissions:
  contents: write

jobs:
  posterior-representation-bridge:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install DeepCash
        run: python -m pip install -e '.[dev]'
      - name: Gate posterior bridge structure
        run: >-
          python -m pytest -q
          tests/test_turn_river_exact_game.py
          tests/test_turn_river_exact_br.py
          tests/test_r4_production_representation_freeze.py
          tests/test_r6_posterior_river_representation_bridge.py
      - name: Run frozen posterior bridge on main only
        if: github.event_name != 'pull_request'
        run: >-
          python tools/run_r6_posterior_river_representation_bridge_v1.py
          --out r6_posterior_river_representation_bridge_v1.json
      - name: Hash result on main only
        if: github.event_name != 'pull_request'
        run: sha256sum r6_posterior_river_representation_bridge_v1.json > r6_posterior_river_representation_bridge_v1.sha256
      - uses: actions/upload-artifact@v4
        if: github.event_name != 'pull_request'
        with:
          name: r6-posterior-river-representation-bridge-v1
          path: |
            r6_posterior_river_representation_bridge_v1.json
            r6_posterior_river_representation_bridge_v1.sha256
          if-no-files-found: error
      - name: Publish immutable hosted evidence pointer to main
        if: github.event_name != 'pull_request'
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p evidence/r6_posterior_river_representation_bridge_v1
          cp r6_posterior_river_representation_bridge_v1.json evidence/r6_posterior_river_representation_bridge_v1/latest.json
          cp r6_posterior_river_representation_bridge_v1.sha256 evidence/r6_posterior_river_representation_bridge_v1/latest.sha256
          printf 'workflow_run_id=%s\nsource_sha=%s\nworkflow=%s\n' \
            "$GITHUB_RUN_ID" "$GITHUB_SHA" "$GITHUB_WORKFLOW" \
            > evidence/r6_posterior_river_representation_bridge_v1/latest_workflow.txt
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add evidence/r6_posterior_river_representation_bridge_v1
          git commit -m "Record R6 posterior representation bridge evidence run $GITHUB_RUN_ID"
          git fetch origin main
          git rebase origin/main
          git push origin HEAD:main
