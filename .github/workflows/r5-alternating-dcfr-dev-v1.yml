name: DeepCash R5 alternating DCFR development v1 INVALIDATED

on:
  workflow_dispatch:

jobs:
  invalidated-v1:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse obsolete numerical generation
        run: |
          echo 'R5 alternating/DCFR v1 was invalidated before result consumption because average-strategy timing was not literature-faithful.'
          echo 'Use r5-alternating-dcfr-dev-v2.yml and docs/R5_ALTERNATING_DCFR_PRECOMMIT_V2_20260816.md.'
          exit 1
